platform:ios, ‘9.0’

inhibit_all_warnings!
 
use_frameworks!

target 'Swift_playground' do
    
    def shared_pods

    pod 'Alamofire',        '~> 4.6.0'
    pod 'SwiftyJSON',       '~> 4.0.0'
    
    
    end
end

target 'Swift_playgroundTests'  do
    shared_pods
end
